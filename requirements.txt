django>=5.0
djangorestframework
django-environ
django-axes
pillow
pdfplumber
requests
psycopg2-binary

# OCR (opcional — apenas necessário para PDFs digitalizados/scaneados)
# Requer também Tesseract e Poppler instalados no sistema operativo:
# choco install tesseract poppler   (Windows)
# sudo apt install tesseract-ocr poppler-utils   (Linux/WSL)
pytesseract
pdf2image