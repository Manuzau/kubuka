# Ambiente virtual Python
venv/
env/
.venv/

# Base de dados local (fallback SQLite automático quando o PostgreSQL não
# está acessível — ver _postgres_reachable em core/settings.py; o nome exacto
# vem de DB_NAME no .env, por omissão "kubuka_db")
db.sqlite3
kubuka_db

# Ficheiros de configuração sensíveis
.env

# Ficheiros de CV submetidos pelos candidatos
media/resumes/

# Logs da aplicação (gerados em runtime — ver LOGGING em core/settings.py)
logs/*.log
!logs/.gitkeep

# Estáticos gerados por `collectstatic` (ver STORAGES/WhiteNoise em core/settings.py)
staticfiles/*
!staticfiles/.gitkeep

# Ficheiros Python compilados
__pycache__/
*.pyc
*.pyo
*.pyd

# Ficheiros do sistema operativo
.DS_Store
Thumbs.db

# Ficheiros do editor de código
.vscode/
.idea/
*.swp